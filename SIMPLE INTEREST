2.SIMPLE INTEREST;

// Online C compiler to run C program online
#include <stdio.h>

int main() {
    float p,r,t,si;

    printf("Enter principal amount: ");
    scanf("%f", &p);
    printf("Enter rate of intrest: ");
    scanf("%f", &r);
    printf("Enter Time (in ye
    si = (p*r*t)/100;
    printf("simple intrest = %2f\n",si);
    return 0;
}

